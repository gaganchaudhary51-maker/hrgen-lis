# Enterprise browser demo

Open `/enterprise.html` after running the server. Sign in through the existing application first, then open the enterprise console in the same browser session. It demonstrates the online API-connected dashboard, new order/billing form, sample desk, quality cards and owner/team rights view.

The service worker caches the shell for offline launch. Clinical mutations must use the server sync contract and must not silently overwrite final verified or released reports.
